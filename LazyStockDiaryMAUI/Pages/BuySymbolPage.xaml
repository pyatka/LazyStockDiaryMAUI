<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LazyStockDiaryMAUI.BuySymbolPage"
             xmlns:vm="clr-namespace:LazyStockDiaryMAUI.ViewModels">
    <ContentPage.BindingContext>
        <vm:BuySymbolViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="{Binding Symbol.Name}"
                    Padding="10,10,10,10"
                    HorizontalOptions="CenterAndExpand" />
                <Label Text="{Binding Symbol.Code}"
                    Padding="10,10,10,10"
                    HorizontalOptions="CenterAndExpand" />
                <Label Text="{Binding Symbol.Currency}"
                    Padding="10,10,10,10"
                    HorizontalOptions="CenterAndExpand" />
            </StackLayout>
            <Entry x:Name="Quantity"
                   HorizontalOptions="FillAndExpand"
                   Keyboard="Numeric"
                   Placeholder="Quantity"
                   Text="{Binding Symbol.Quantity}"/>
            <Entry x:Name="Price"
                   HorizontalOptions="FillAndExpand"
                   Keyboard="Numeric"
                   Placeholder="Price"
                   Text="{Binding Symbol.Price}"/>
            <DatePicker
                Date="{Binding Symbol.Date}"/>
            <Button Text="Buy"
                    Command="{Binding BuySymbol}"
                    CommandParameter="{Binding Symbol}"
                    HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>